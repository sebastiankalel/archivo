<!--
form_for @file_record.steps.build
form_for @file_record, @step

f.assoc
-->
<%= form_for(@step) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :file_record_id, :value => @file_record.id  %><br>
    <%= f.hidden_field :person_id, :value => 0  %><br>
    <%=  %>
  </div>
  <div class="field">
    <%= f.label :office_id %><br>
    <%= collection_select(:step, :office_id, Office.all,:id,:name) %>
  </div>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>